<div class="main posts-index">
  <div class="container">
    <% if @diaries.present?%>
    <% @diaries.each do |diary| %>
    <div class="posts-index-item">
      <div class="post-right">
        <div class="post-project-name">
          <%= link_to(diary.item, "/diaries/#{diary.id}") %>
        </div>
      </div>
    </div>
    <% end %>
    <%else%>
      <%= "#{@purchase_date}には何も購入していません" %>
    <% end %>
  </div>
</div>
